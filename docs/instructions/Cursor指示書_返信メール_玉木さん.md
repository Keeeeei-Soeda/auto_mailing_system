# Cursor 指示書：玉木さんへの返信メール送付（8/9番組の打ち合わせ日程）

## 目的
玉木和彦さんからのメール（件名「玉木です。」）に対し、8/9番組の打ち合わせ日程などを回答する返信を送付する。**既存スレッドへの返信**として送ること。

## 送信内容（この文面をそのまま使用）
- **差出人**：k.soeda@medi-canvas.com（株式会社メディキャンバス 副田 渓）
- **宛先（To）**：tamauzura99@gmail.com（玉木 和彦 様）
- **CC**：pharnewton@gmail.com（元スレッドのCCに合わせて設定。不要なら削除可）
- **件名**：Re: 玉木です。
- **返信対象**：スレッド「玉木です。」への返信として紐付ける
  - `threadId`：`19f605da3923d395`
  - `replyToMessageId`（In-Reply-To/References用の元メッセージID）：`19f605da3923d395`
- **本文**：

```
玉木様

お世話になっております。
株式会社メディキャンバスの副田です。
ご連絡ありがとうございます。大学の課題レポートで「よりそい」を取り上げていただいたとのこと、大変光栄です。

番組内の構成や「質疑応答」の位置づけについて、一度オンラインで打ち合わせできればと思います。私からは以下の候補をお出ししますので、ご都合のよい時間帯をお知らせいただけますでしょうか。

・7月22日（水）13:00〜15:00
・7月23日（木）13:00〜18:00

＜確認事項について＞
① ご紹介の肩書：「薬剤師／株式会社メディキャンバス 代表取締役」で問題ありません
② 番組タイトル：ご提示の内容で問題ありません

モニター募集のPRについてもお気遣いいただきありがとうございます。せっかくの機会ですので、ぜひ活用させていただければと思います。

引き続き、どうぞよろしくお願いいたします。

株式会社メディキャンバス
代表取締役 副田 渓
```

## 実装方針
- Python で Gmail API を用いてメールを作成する。本文はプレーンテキスト。
- 認証は既存の OAuth クライアント／トークン（`k.soeda@medi-canvas.com` の `gmail.compose` または `gmail.send` スコープ）を再利用する。無ければ新規取得する。
- **既存スレッドへの返信として送ること。** MIME に `In-Reply-To` と `References` ヘッダ（元メッセージID `19f605da3923d395`）を設定し、Gmail API の `threadId`（`19f605da3923d395`）を指定して送信・下書き作成する。
- **既定の挙動は「下書き（draft）作成」とすること。** 宛先・CC・本文を目視確認したいため、いきなり `send` はしない。
- 確認後に送信できるよう、`--send` フラグ等で `send` にも切り替えられるようにする。

## 実行手順
1. スクリプトを実行し、上記の宛先（To/CC）・件名・本文・スレッド紐付けで Gmail に**下書き**を作成する。
2. Gmail の下書きを開き、宛先・CC・本文と、スレッド「玉木です。」に返信として紐付いていることを確認する。
3. 問題なければ下書きから送信、またはスクリプトを送信モードで再実行する。

## 完了条件
- 指定の宛先（To：tamauzura99@gmail.com／CC：pharnewton@gmail.com）・件名・本文を含む下書きが Gmail 上に作成されている。
- スレッド「玉木です。」の返信として紐付いている（新規スレッドになっていない）。

## 注意点・補足
- 新規スレッドにならないよう、必ず `threadId` と `In-Reply-To`/`References` の両方を設定すること。
- CC の pharnewton@gmail.com は副田さん自身の別アドレス。ループに残す必要がなければ削除してよい。
